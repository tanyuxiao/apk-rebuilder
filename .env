PORT=3005
HOST=127.0.0.1
APK_REBUILDER_MODE=dev

# Host integration
HOST_API_BASE=http://localhost:8081
HOST_AUTH_DEBUG=true

# Note: Tools (apktool, zipalign, apksigner) are now managed via 
# environment variables in docker-compose.yml or System PATH.
