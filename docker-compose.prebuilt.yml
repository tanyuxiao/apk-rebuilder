services:
  apk-rebuilder:
    image: ${APK_REBUILDER_IMAGE:-ghcr.io/tanyuxiao/apk-rebuilder:latest}
    pull_policy: always
    ports:
      - '3000:3000'
    environment:
      - PORT=3000
      - HOST=0.0.0.0
